# web/

Aplicação Next.js do Dinheiritos (a criar — Fase 2 do [ROADMAP](../ROADMAP.md)).

Scaffold previsto:

```bash
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir
```

Requisitos: `output: 'export'` no `next.config` (GitHub Pages), `@supabase/supabase-js`, Recharts. Variáveis em `.env.local` (ver `../.env.example`).
